# Лог промптов

## 2026-09-19 / 20 — сессия «mvp pipeline with minimal models»

1. «Давай пока сделаем MVP проекта — минимум моделей/витрин для полного демо от ingest до ML, минимальный
   сэмпл, идемпотентная загрузка, полная очистка в Makefile (всё кроме raw / только raw). Создай decisions.md,
   TODO.md (+ gh-issues), README, architecture, runbook, DEMO.md» → первый MVP-план (`333f30c`).
2. «Как будут разделяться обучение и работа модели в проде (напомни термин)?» → training vs inference,
   training-serving skew → ADR-13.
3. «Можно не использовать worktree, если нет конкурирующих веток? Смержи, удали worktree, upsert в память и
   правила» → worktree удалён; далее «работать в ветке feat/…, но в текущем каталоге — GitHub-flow/TBD».
4. «Корректировки к ревизии — только дельта» (30 мин; dlt не в MVP; Evidently узко; champion до записи;
   Airflow/Cosmos репликами; оценка Justfile; CI/observability кратко; аудит версий) + полный текст
   «Ревизия и доработка плана проекта» (§0–§14) → read-only аудит, ответы на блокирующие вопросы
   (Evidently — нет; Justfile — да; observability = UI + Telegram; snapshot 5k + git-lfs) → правки docs в
   `feat/mvp-plan-v3`.
