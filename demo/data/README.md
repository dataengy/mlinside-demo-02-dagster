# demo/data

Данные демо 1, генерируются и в git не попадают (кроме этого README):

- `mart_order_features.parquet` — выгрузка `marts.mart_order_features` из [`data/olist.duckdb`](../../data/olist.duckdb)
  (`just demo-1-prepare`, скрипт [`demo/materials/1/export_mart.py`](../materials/1/export_mart.py)).
